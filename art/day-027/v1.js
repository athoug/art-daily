const width = 400;
const height = 400;

let r = 0;

let red;
let green;
let blue;

function setup() {
	createCanvas(width, height);
	smooth();
	noStroke();
	rectMode(CENTER);
}

function draw() {
	background(255);
}
