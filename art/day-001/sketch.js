function setup() {
	// first step is creating a canvas for our drawings
	createCanvas(400, 400);
}

function draw() {
	// set the background color
	background(220);
	// lets walk through teh get started page of p5.js
	// The line you just added draws an ellipse, with its center 50 pixels over from the left and 50 pixels down from the top, with a width and height of 80 pixels.
	ellipse(50, 50, 80, 80);
}
