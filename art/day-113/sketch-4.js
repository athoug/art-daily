const width = 400;
const height = 400;
const halfW = width / 2;
const halfH = height / 2;

function setup() {
	createCanvas(width, height);
	frameRate(30);
	background(250);
}

function draw() {
	// start drawing
}
